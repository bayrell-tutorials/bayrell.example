{
  "name": "hello-world",
  "version": "0.1.0",
  "scripts": {
    "build": "parcel build src/index.html --public-url / --out-dir ./web --cache-dir ./var/parcel",
    "watch": "parcel watch src/index.html --public-url / --out-dir ./web --cache-dir ./var/parcel"
  },
  "dependencies": {
    "bayrell": "^0.8.3",
    "bayrell-lang-nodejs": "^0.9.3",
    "bayrell-runtime-nodejs": "^0.9.3"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "sass": "^1.26.9"
  }
}
